# cpp-template
C++ template setup with `CMake` on macOS.

- Project was created on MacOS, has not been tested on Windows or Linux
- Run `./setup` to setup the project
- use `./run` for running afterwards